import React from 'react'
import { PresaleInnerBlock, ClaimButton } from '../styled'

const Claim = () => {
   return (
      <PresaleInnerBlock>
         Claim
         <ClaimButton>Claim</ClaimButton>
      </PresaleInnerBlock>
   )
}

export default Claim